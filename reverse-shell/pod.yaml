apiVersion: v1
kind: Pod
metadata:
  name: token-dumper
spec:
  containers:
  - name: dumper
    image: alpine:latest
    command: ["/bin/sh", "-c", "cat /var/run/secrets/kubernetes.io/serviceaccount/token; sleep 30"]
  restartPolicy: Never
